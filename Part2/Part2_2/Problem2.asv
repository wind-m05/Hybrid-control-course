clear
close all
clc

%% Dynamics
A1 = [1/2 1/2; -1/4 -5/4];
A2 = [5/6 5/3; -2/3  1/6];
B1 = [ 0; 3];
B2 = [-2; 1];
C1 = [0 1];
C2 = [1/3 2/3];

%% LMI
% Variables
Zvar = sdpvar(2,2);
Y1var = sdpvar(1,2);
Y2var = sdpvar(1,2);

% Conditions
Lp = Zvar >= 1e-9;
% Lf1 = Zvar - (Zvar*A1' + Y1var'*B1')*inv(Zvar)*(A1*Zvar + B1*Y1var) <= 0;
% Lf2 = Zvar*A2' + A2*Zvar + Y2var'*B2' + B2*Y2var <= 0;
Lf1 = [Zvar ];
